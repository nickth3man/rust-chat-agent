# Single source of truth for the Rust version — rustup (locally and on CI
# runners) auto-installs this exact toolchain. Bump deliberately, then make
# sure `just ci` still passes.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy", "llvm-tools-preview"]
